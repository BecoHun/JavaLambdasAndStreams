1.
Which two of the following functional interface declarations are correct?
1.
@FunctionalInterface
interface Action {
    void execute();
    default void refresh(){}
}
2.
@FunctionalInterface
interface Action {
    void execute();
    default void refresh();
}
3.
@FunctionalInterface
interface Action {
    void execute();
    static void refresh(){}
}
4.
@FunctionalInterface
interface Action {
    void execute();
    void refresh(){}
}
1 and 3
Only #1 and #3 are syntactically correct. #2 contains an incorrect declaration of a default method, and #4 contains two abstract methods.

2.
Which of the following method declarations can be used independently in a functional interface? 
private static void method(){}
void method();
The members of an interface cannot be protected. An interface's abstract methods cannot have a body.

3.
What independent changes are needed to make the declaration of the functional interface correct?
@FunctionalInterface
interface A {
    void methA();
    public abstract void methB();
}
Delete declaration methA
Delete declaration methB
Deleting one of these methods yields the correct syntax for the functional interface. 
Adding the keyword static before any of them will cause a compilation error because static methods in interfaces should have bodies.

4.
Which of the following implementations of a functional interface is correct?
@FunctionalInterface
interface Service {
    int doService(int value);
}
Service s3 = i -> i > 3 ? 3: -1;
This implementation is syntactically correct: It takes a single argument and returns an int value, just as the method specification states.

5.
Which two of the following implementations of a functional interface are correct?
@FunctionalInterface
interface Action {
    void doAction(int num);
}
Action action = i -> System.out.println(i);
Action action = (int i) -> System.out.println(i);
That’s right! The other two options are incorrect. 
The option "Action action = Integer i -> System.out.println(i);" does not pass the syntax check. 
The option "Action action = (Integer i) -> System.out.println(i);" has the Incompatible parameter type–expected int but found Integer.

6.
Which of the following two statements are true?
The Predicate interface returns a boolean.
The Predicate interface has a method named test().
That’s right!

7.
What is the output of the following code implementation?
record Person(int id, String name){} 
// code fragment
List<Person> persons = List.of(new Person(5, "jack"),
        new Person(7, "john"),
        new Person(3, "kurt"),
        new Person(1, "arnold")
);
Predicate<Integer> is = i -> i > = 3;
for (Person person :persons) {
  System.out.print(is.test(person.id()) + " ");
}
true true true false
You will see the results if the given id value is greater than or equal to 3.

8.
Which of the following implementations of the Consumer interface is correct?
List<Integer> ints = new ArrayList<>();
    Consumer<List<Integer>> consumer = o -> ints.add(3);
The Consumer interface describes a functionality that takes a parameter, performs a useful task, and returns nothing. 
All other cases specify functionality with no side effects, generally making them useless.

9.
Which two of the following lines do not cause compilation errors?
class Person { 
    public Person() {}
    public Person(final String name) {}
} 
Function<String, Person> stringToPerson = Person::new;
Supplier<Person> personSupplier = Person::new;
These variants are covariant to the specifications of Person constructors.

10.
Which of the following uses of a method reference is correct?
class FunctionHolder {
    public double function(final int a, boolean b) {
        return 0; 
    } 
} 
@FunctionalInterface 
interface FirstInterface <T, U, R> {
    R function(T firstArgument, U secondArgument); 
} 
FunctionHolder holder = new FunctionHolder();
FirstInterface<Integer, Boolean, Double> first = holder::function;
Yes, this is the correct parameterization for achieving covariance with the referenced method.

11.
Which of the following method references can be used to initialize the type reference DoublePredicate?
boolean a(double a){return true;}
A DoublePredicate reference accepts a value of the double type and returns a Boolean.

12.
Which of the following code fragments does not cause compilation errors? 
int x[] = {1};
Consumer<Integer> consumer = n -> n += x[0]+=n;
You cannot reassign the value of a captured variable, but you can change the state of the object referenced.

13.
What will be the output of the following code implementation?
interface Suit {
    String convertToSuit(int x);
}
class Quiz15 { 
    public static void main(String[] args) {
        Suit suit = new Suit() {
            String[] suits = {"Diamonds", "Hearts", "Clubs", "Spades"};
            public String convertToSuit(int n) {
                return (n >= 1 && n < suits.length + 1)
                    ? suits[n - 1]
                    : null;
            };
        };
        System.out.println(suit.convertToSuit(3));
    }
}
Clubs
An instance of an anonymous class implements a functional interface and provides an implementation of the convertToSet() method.

14.
What will be the output of the following code implementation?
int summator = 5; 
IntUnaryOperator sum = i -> summator += i; 
System.out.println(sum.applyAsInt(2)); 
Compilation will fail.
There will be a compilation error because the variable used in the lambda expression should be final or effectively final.

15.
Which two of the following code fragments will compile when filling in the blank space?
Map<Integer, Object> map = Map.of(1, 3, 5, 7);
__________.forEach(System.out::print);
map.keySet()
map.values()
This particular forEach method, taking a simple Consumer, is applicable to collections of keys and values.

16.
What will be the output of the following code implementation?
Map<String, Integer> map = new HashMap<>();
map.compute("y", (k, v) -> v == null ? 1 : 0);
map.compute("z", (k, v) -> v == null ? 2 : 0);
map.computeIfPresent("z", (k, v) -> v != null ? 3 : 0);
map.computeIfAbsent("y", v -> v != null ? 4 : 0); System.out.println(map.values());
[1, 3]
That’s right!

17.
Which of the following code fragments does not cause compilation errors?
class Person{
    private Optional<Car> car;
    public Optional<Car> getCar() {
        return car;
    }
}
class Car{
    private Optional<Insurance> insurance;
    public Optional<Insurance> getInsurance() {
        return insurance;
    }
}
class Insurance{
}
A.
Optional<Person> person = Optional.of(new Person());
Insurance insurance = person.flatMap(Person::getCar)
    .flatMap(Car::getInsurance)
    .get();
In this case, you need the flatMap operation to avoid getting Optional‹Optional‹?››.

18.
What is the output of the following code implementation?
List<String> words = 
        List.of("john", "jack", "phillip", "antoni", "pit");
List<String> result = words.stream()
        .filter(w -> w.length() > 4)
        .map(w -> w.substring(3))
        .toList();
System.out.println(result);
[llip, oni]
The stream’s input is a list of 5 strings, the elements of which are filtered by length–should be longer than 4 characters–strings "phillip" and "antoni" passed the filter. 
Then the string is trimmed from left starting from the character with index 3. The result is formed as a List and printed in the console output.

19.
Which of the following statements are true?
A stream is not a data structure that stores elements.
A stream accepts elements from arrays.
An operation with a stream produces some result but does not change the source of the stream.
A stream is not a data structure that stores elements; a stream simply transfers elements from some source 
(for example, data structure, array, data input/output stream) through a pipeline of computing operations. 
An operation with a stream produces some result but does not change the source of the stream.

20.
What is the output of the following code implementation?
List<String> words = 
        List.of("john", "jack", "phillip", "antoni", "pit");
List<Integer> result = words.stream()
        .filter(w -> w.compareTo("melani") > 0)
        .map(w -> w.length())
        .toList();
System.out.println(result);
[7, 3]
The stream's input is a list of 5 strings, the elements of which are filtered based on the lexicographic comparison with the string "melani." 
The strings "phillip" and "pit" passed the filter because they are lexicographically "greater." 
Then the method for getting the length of such a string is applied. The result is formed as a List and printed in the console output.

21.
Which of the following statements are true? 
A stream can have an infinite number of elements.
Stream elements are used only once during stream life.
Streams can have an infinite number of elements, unlike collections. Stream elements are visited only once during stream life. 
That is, to return to the source elements that have already been used by stream operations, you need to generate a new stream.

22.
What is the output of the following code implementation?
List<String> words = 
        List.of("john", "jack", "phillip", "antoni", "pit");
List<Character> result = words.stream()
        .filter(w -> w.matches("[a-p]+"))
        .map(w -> w.charAt(2))
        .toList();
System.out.println(result);
[h, c, i]
The stream’s input is a list of 5 strings, the elements of which are filtered by the regex pattern. 
The pattern "[a-p]+" as a filtering condition leaves those strings that consist only of the characters a, b, c, d … o, p and have any length. 
The strings "john", "jack", and "phillip" passed the filter. 
Then the char with index 2 is used from each string. The result is formed as a List and printed in the console output.

23.
Which of the following statements are true? 
The terminal operation converts a set of stream elements into a result.
The stream does not start executing until a terminal operation is defined.
The terminal operation is a collection of stream elements into a certain data set. 
Streams must have a source–the stream does not start executing until a terminal operation is defined. 
Classes for working with streams are located in the package java.util.stream.

24.
Which of the following statements can be used to create a stream?
var stream1 = Stream.iterate("", s -> s += 77);
var stream2 = Arrays.stream(new int[]{});
The generate() and iterate() static methods of the Stream interface allow you to create endless data streams using a Supplier type supplier or 
based on a given UnaryOperator operation. 
The Method stream() of class Arrays returns a sequential IntStream with the specified array as its source. 
The variant with {1, 2, 3} will generate the compile error, because array initialization is not allowed where the Supplier type parameter is expected. 
The Stream interface doesn’t have the range() method.

25.
What is the output of the following code implementation?
var result = IntStream.range(1, 4)
        .map(n -> ++n)
        .sum();
System.out.print(result);
9
The stream’s input is an integer range from 1 (included) to 4 (not included)—generated sequence: 1, 2, 3. 
The next step is to use pre-increment (generated sequence: 2, 3, 4) rather than the sum of the value. The result is printed in the console output.

26.
What is the output of the following code implementation?
var i = 1;
var result = IntStream.generate(()-> ++i)
        .limit(3)
        .sum();
System.out.print(result);
Compilation fails
There is compilation error, because the local variable i referenced from a lambda expression must be final or effectively final.


